<h1><%= @song.name %></h1>

<!-- Add code here to list a category's posts. -->
<h3><%= @song.artist_name %></h3>
<ul>
  <% @song.songs.each do |p| %>
    <li><%= link_to p.title, song_path(p) %></li>
  <% end %>
</ul>
